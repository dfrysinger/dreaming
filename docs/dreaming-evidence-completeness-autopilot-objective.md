Work from `docs/dreaming-evidence-completeness-design.md` in
`/Users/dfrysinger/code/dreaming`.

Outcome: the Mac mini incrementally finishes and maintains trustworthy Copilot
skill-usage coverage for the MacBook, exact Git-proven historical names are
attributed without guesses, the read-only dashboard works at
`https://mac-mini.hornbill-dragon.ts.net:47673/` for trusted tailnet peers, and
a finite audit records which other dashboard claims have live producers.

Follow the reviewed plan in order and keep its Definition of Done and proof
baton accurate. Use the charter's required process skills and push policy.
Keep the implementation small: no database, daemon, byte-offset parser state,
runtime Git inference, accounts, reverse proxy, non-loopback bind, multi-vendor
usage generalization, unrelated queue redesign, or automatic repair of every
gap the audit finds.

Finish only when every item in the "Definition of Done: Dreaming evidence
completeness" section is verifiably met. Do not substitute partial fixtures,
code inspection, an incomplete catch-up, or a reachable but nonfunctional
dashboard page for the observable completion evidence named there.
