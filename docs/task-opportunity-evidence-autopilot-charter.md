# Task opportunity evidence autopilot charter

## Workspace and authority

- Work only in `/Users/dfrysinger/code/dreaming` on
  `feature/multi-cli-dreaming`.
- Follow the current baton in
  `docs/aggressive-skill-portfolio-governance-autopilot-plan.md`.
- The reviewed work order is
  `docs/task-opportunity-evidence-design.md`.
- Completion is governed by `## Definition of Done` in that design.
- Never push. Keep every commit local and preserve unrelated user changes.
- Use the existing authenticated transcript path. Do not add another
  transport, corpus walker, scheduler, or deterministic semantic classifier.
- Keep the Mac mini as the sole scheduled analysis, evidence, evaluation, and
  deployment owner. The MacBook Pro remains the transcript source and
  learned-skill destination.

## Required process skills

### Governing skill

- `dreaming-proof-first` owns the tracer, effectiveness decision, focused
  hardening, proof ledger, and the gate into release work. Invoke it after
  compaction when it is no longer active and follow its protocol until the
  task-opportunity Definition of Done is complete.

### Execution skills

- `code-review` owns paired review and adversarial adjudication of the frozen
  implementation diff. Apply only confirmed findings in successor commits.
- `writing-great-skills` owns the quality criteria for any recurring
  opportunity that becomes a skill candidate.
- `skill-create` or `skill-manage` owns creation or repair respectively after
  independent recurrence authorizes that action.
- `development-loop` owns deterministic closure and the final release phases
  after the proof-first effectiveness gate passes.
- `behavior-validation` owns installed end-to-end evidence, including bounded
  profiling and natural cadence.
- `visual-proof` owns final desktop and narrow dashboard evidence.
- `live-governance-lever-verification` owns installed halt, sole-owner,
  rollback, and restore proof.

Invoke an execution skill only when entering the phase it owns. Do not create
a candidate from one occurrence merely to advance the run.

### Context skill

- `self-compact` owns every same-session compaction. Before compacting, persist
  the current baton and proof references, confirm this hourly reminder remains
  live, and use the skill's verified continuation protocol. Never use a bare
  compact.

## Autonomy mandate

Continue without routine approval through review closure, independent
recurrence, candidate creation or repair, evaluation, installation, natural
cadence, browser proof, rollback, local commits, and reminder cleanup.

Stop only for destructive or irreversible action, unavailable credentials or
authority, conflicting acceptance criteria, or a material redesign outside
the reviewed work order. Preserve strict validation failures as evidence
rather than weakening them to make a tracer pass.

## Plan hygiene

- Maintain the aggressive-governance plan's Current baton as the single source
  of implementation and proof status.
- Keep the immutable review target `7cda1a1..3e586f2` unchanged. Any confirmed
  fix becomes a successor commit and reruns the proof affected by that fix.
- Treat a task profile as semantic structure, not recurrence. Require
  independent observations before candidate mutation.
- Use LLM interpretation for task meaning. Deterministic code may validate
  identity, ordering, receipts, limits, and authority, but must not replace
  semantic transcript interpretation without explicit user approval.
- Keep the full-review limit at 25 while profiling remains independently
  bounded at its configured capacity.
- Run the smallest targeted checks first. Do not use repeated full self-tests
  or natural cadence as the debugging loop.
- Retain honest failed and inconclusive evidence, including strict event-order
  refusal and the explicitly labelled replay seam.
- Do not broaden this run into unrelated portfolio governance, model routing,
  transcript transport, or security hardening.

## Installation and push policy

- Use the existing installer, halt, self-test, enable, rollback, publication,
  and restore boundaries.
- Install only a frozen, reviewed candidate behind halt.
- Transfer reviewed commits between the user's computers without remotes.
- Never push any branch, commit, or tag.

## Completion

The run is complete only when every checkbox under `## Definition of Done` in
`docs/task-opportunity-evidence-design.md` is supported by retained evidence,
all implementation and proof references are committed locally, no in-scope
must-fix review finding remains, and this hourly reminder has been stopped.
