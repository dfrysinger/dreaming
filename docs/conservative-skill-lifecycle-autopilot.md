# Conservative skill lifecycle shadow milestone charter

## Objective

Achieve the Definition of Done in
`docs/conservative-skill-lifecycle-design.md`, the "Definition of Done: Shadow
candidate admission and evaluation" section: candidate state,
recurrence-based admission, and focused routing and task-value evaluation work
end to end without granting production mutation authority. Keep working
through the milestone; finish only once every item in that section is
verifiably met.

## Charter

Keep building against the plan at
`docs/conservative-skill-lifecycle-design.md` through the shadow candidate
admission and evaluation milestone in `/Users/dfrysinger/code/dreaming`.
Follow the required process skills below. If a reminder arrives while you are
making a user-directed edit to the design, brief, or charter, finish the
current coherent edit and persist it before reconciling against the
authoritative file. Never replace an in-flight revision with an older
persisted version. Use rubber-duck to align on paths forward whenever stuck.
Keep the design and this baton up to date so a future agent can resume from
them. Use subagents for genuinely independent work, not duplicate passes.
Do not push. Keep work local for this run. Decide every reversible question
yourself with rubber-duck rather than asking the user. Freely use real model
tokens within reason for focused evaluation and review, but do not run the
complete portfolio matrix or the installed six-hour self-test while the host
activation gate remains closed. Keep the halt switch present and do not enable
Dreaming. Stay on this course until the objective's Definition of Done, the
"Definition of Done: Shadow candidate admission and evaluation" section, is
met.

### Required process skills

- **Governing:** `/dfrysinger-skills:development-loop` owns phase order,
  deterministic checks, live proof, review, and completion. Invoke it at run
  start and after compaction when it is no longer active.
- **Execution:** `/deep:behavior-validation` owns the final real runtime
  behavior proof after deterministic and contract tests pass.
- **Context:** `/dfrysinger-skills:self-compact` owns compaction at the
  governing workflow's compaction points or when context becomes noisy.
  Persist the current phase, candidate identity, checks, review state, and
  live-proof receipt before invoking it. Do not compact while live proof is in
  progress.

## Current baton

- **Lane:** critical.
- **Workspace:** `/Users/dfrysinger/code/dreaming`.
- **Branch:** `feature/multi-cli-dreaming`.
- **Push policy:** local only.
- **Design:** `docs/conservative-skill-lifecycle-design.md`.
- **Scope:** shadow candidate admission and evaluation milestone only.
- **Order:** candidate and lifecycle state, recurrence-based admission, then
  focused routing and task-value evaluation.
- **Activation gate:** closed for this run. This milestone never certifies the
  installed generation, writes an activation receipt, removes the halt switch,
  or enables a LaunchAgent. Reopening the gate is a separate user-initiated
  milestone.
- **Standing safety state:** keep
  `/Users/dfrysinger/.copilot/skill-state/skill-review/disable-daemon` present;
  do not enable LaunchAgents or mutate publisher state.
- **Landed baseline:** `9ce1018`.
- **Current phase:** milestone complete and locally landed.
- **Completed:** the reviewed work order is committed at `367ebe4`; candidate
  records, immutable packages, lease-checked transitions, recurrence,
  deferred-create collection, and read-only dashboard contracts are
  implemented on the working tree with targeted deterministic tests passing.
  Exact-identity catalog-plus-candidate routing, paired task-value evaluation,
  sealed receipts, copied native Codex skill projection, single-source native
  discovery, complete-read load attestation, native command accounting, and
  explicit Codex web-search disabling are also implemented.
- **Remaining:** none within this charter. Production activation remains a
  separate user-initiated milestone.
- **Deterministic proof:** candidate lifecycle `10`, core `32`, dashboard
  contracts `54`, dashboard API `102`, evaluator `23`, harness `31`, vendor
  adapters `12`, and mutation boundary `8` checks pass on the final tree.
  Process cleanup, Python compilation, and shell syntax checks also pass.
- **Validation candidate:** the final exact-tree proof uses candidate
  `sha256:05de0b79c5ad6d20d878af469b4f5d2755720bd0a6eb453d8a101de9ff689d2f`,
  catalog
  `sha256:6d8c7b5bd73b40ade82fc3ea5bf86593f8fec87653bc918b825ac14f85a3f996`,
  environment
  `sha256:0135ec67edbaa7bf0d125d41b958a1cdd556bcd274a13a223ba306366003f0e2`,
  suite
  `sha256:6a1ad7ca03c80023309578cfc53941b6d9876cb9b14009da2e9053ffe1795451`.
- **Live-proof receipt:** final review-fix evidence is under
  `/Users/dfrysinger/.copilot/session-state/c8b94df9-d6e2-5cd7-a707-155405e27d8f/files/conservative-shadow-final-fix-proof/`.
- **Focused real-model proof:** the complete six-trial Codex
  `gpt-5.4-mini` run passed both routing and task-value gates. Run identity is
  `sha256:6216ef06ec0e2ddc75866cd03ca48a18172d96cd729ede14f176115baf2c82ab`;
  result identity is
  `sha256:0af389858e5e0f98f2551429fbcee3c663b2fd61cb868e01ed9566f6bf9c4a90`;
  the authoritative receipt basename is
  `46fa118a2098d0ee7350b41d1168c69bb0c138d3ef0e36aa9d7f6c6636d9ceea`.
  The earlier Generation 10b proof remains historical but is stale for landing
  because executable behavior changed afterward.
- **Live-proof execution:** the original selector retained eight scenarios
  across all five high-risk areas. Seven original scenarios were independently
  judged `SUPPORTED`; the original `SC-016` was `FLAKY`. Final exact-tree
  repair scenarios `SC-016` and `SC-018` both completed with infrastructure
  status `OK` and independent final verdict `SUPPORTED`. Browser-visible
  read-only candidate list and detail surfaces also have real Chrome proof.
- **Implementation review:** bounded round two resolved all seven prior
  findings. One additional bounded metric-integrity finding was fixed by
  re-deriving `candidate_loaded` from sealed load evidence; both Claude Opus 5
  and GPT-5.6 Terra closed the finding with no regressions.
- **Completion authority:** the "Definition of Done: Shadow candidate
  admission and evaluation" section in the design.
