# Dreaming review backlog capacity autopilot charter

## Workspace and authority

- Work only in `/Users/dfrysinger/code/dreaming` on
  `feature/multi-cli-dreaming`.
- The reviewed work order is
  `docs/dreaming-review-backlog-capacity-design.md`.
- Completion is governed by
  `## Definition of Done: Dreaming review backlog capacity` in that document.
- Never push. Commits remain local on the MacBook and Mac mini.
- Preserve unrelated work. Do not rewrite, discard or hide user changes.
- Keep the implementation to the reviewed architecture. If evidence requires a
  material redesign, update and review the design before implementing it.
- A separate report-only governance preview may proceed under
  `docs/aggressive-skill-portfolio-governance-design.md` without changing this
  branch tip or tracked working tree, the installed generation, or launchd.
  Live state may change only through the existing installed capacity owner.

## Required process skills

### Governing skill

- `development-loop` owns phase order, the plan baton, red-green-refactor
  implementation, validation, review closure and the final completion gate.
  Invoke it at the start of the fresh session and follow its protocol until the
  named Definition of Done is complete.

### Execution skills

- `dual-review` owns the paired implementation review after deterministic
  validation and again only as required to close material findings.
- `behavior-validation` owns end-to-end behavioral evidence for installed
  cadence, bounded reviews, overlap/halt behavior and truthful dashboard
  capacity states.
- `visual-proof` owns the final desktop and narrow-viewport proof for the
  Overview capacity presentation.
- `live-governance-lever-verification` owns installed proof that halt,
  enablement and rollback still control the more frequent scheduled owner.

Invoke an execution skill only when entering the phase it owns. Do not invoke
planning or explanation skills as standing dependencies.

### Context skill

- `self-compact` owns every same-session compaction. Before compacting, persist
  the current plan baton and proof references, confirm the hourly charter
  reminder remains live, and use the skill's verified continuation protocol.
  Never use a bare compact during this run.

## Autonomy mandate

Continue without waiting for routine approval. Investigate, implement, test,
review, install and gather proof under the work order. Make reasonable,
reversible decisions within its explicit scope.

Stop only for a destructive or irreversible action, missing credentials or
authority that cannot be recovered, conflicting acceptance criteria, or a
material redesign beyond the reviewed architecture. Record any real blocker
with the failed command, retained evidence and smallest safe next action.

## Plan hygiene

- Maintain one current plan baton using `development-loop`; do not create
  competing plans.
- Keep check IDs CHK-C1 through CHK-C7 and the Definition of Done accurate as
  evidence lands.
- Treat tests, live receipts and browser captures as evidence, not prose
  substitutes.
- Use the smallest targeted existing checks first, then the required installed
  and end-to-end validation.
- Run subagents only when a named process skill requires them or a task needs a
  genuinely separate context. Give each a bounded objective and do not
  duplicate their scope in the parent.
- Do not broaden this branch into review policy, model routing, transcript
  indexing, learned-skill usage, watchdog presentation or unrelated dashboard
  work. Governance preview work belongs only in its isolated worktree until
  capacity proof is terminal and retained.

## Installation and push policy

- Use the repository's existing installer, self-test, halt, enable and rollback
  boundaries.
- Keep the Mac mini as the sole active scheduled Dreaming owner.
- Transfer reviewed commits without pushing.
- Never push any branch, commit or tag to a remote.
- Do not enable a candidate that has not passed installed self-test.

## Completion

The run is complete only when every checkbox under
`## Definition of Done: Dreaming review backlog capacity` is supported by
retained evidence on the reviewed tree, the paired implementation review has
no unresolved in-scope must-fix finding, the Mac mini runs the final installed
generation, and the hourly charter reminder has been stopped.
